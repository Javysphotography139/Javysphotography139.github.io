# Git ignore for a static site (GitHub Pages) with Node-based scripts

# Keeps site assets (images, CNAME, CSS/JS) tracked while ignoring OS/editor cruft and local tooling caches
package-lock.json

# ——— Node / tooling ———
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log
.eslintcache

# Instrumentation / coverage
lib-cov
coverage/

# Build/intermediate outputs (kept local)
.grunt/
dist/
.tmp/
build/Release

# Dependency caches
.npm/
.pnpm-store/
.cache/
.parcel-cache/

# ——— Bower (legacy) ———
bower_components/
.bower-cache/
.bower-registry/
.bower-tmp/

# ——— macOS ———
.DS_Store
.AppleDouble
.LSOverride
# Icon must end with two \r
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# ——— Linux ———
*~
.directory
.Trash-*

# ——— Windows ———
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msm
*.msp
*.lnk

# ——— Editors / IDEs ———
.vscode/
.settings/
.idea/
# Sublime
*.sublime-workspace
*.sublime-project
*.tmlanguage.cache
*.tmPreferences.cache
*.stTheme.cache
sftp-config.json

# ——— Styles / preprocessors ———
.sass-cache/
*.css.map

# ——— Dropbox ———
.dropbox
.dropbox.attr
.dropbox.cache

# ——— Env files (keep examples) ———
.env
.env.local
.env.development
.env.test
.env.production
.env.*
!.env.example

# ——— Logs (generic) ———
logs
*.log
pids
*.pid
*.seed

# NOTE:
# - Do NOT ignore images/ or images/optimized/; they are used by the site and should remain tracked.
# - Keep CNAME tracked for custom domain on GitHub Pages.
